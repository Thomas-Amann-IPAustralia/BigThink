# ==============================================================
# BigThink — core dependencies
#
# Deliberately small. Everything here installs in seconds on a
# GitHub Actions runner with no compiler and no model download,
# which is what keeps the pipeline runnable at zero cost.
# Heavy ML dependencies are in requirements-ml.txt.
# ==============================================================

# Configuration
pyyaml>=6.0

# HTTP
requests>=2.31

# Analytical store (BasicInfraSuggestion.md)
duckdb>=1.0

# Cloudflare R2 client (S3-compatible; BasicInfraSuggestion.md). Pure Python,
# installs in seconds. Only touched when storage.r2.enabled is true.
boto3>=1.34

# Vector maths, growth-curve fitting, burst detection
numpy>=1.26

# Dashboard point-cloud layout (src/dashboard.py). Pure-wheel installs on a
# standard runner — no compiler, no torch, no model download — so it stays
# with the core set rather than requirements-ml.txt, which exists for the
# heavier torch/model-download case. If unavailable at runtime, the dashboard
# falls back to a numpy-only PCA projection rather than failing the build.
umap-learn>=0.5
scikit-learn>=1.4

# Testing
pytest>=8.0
